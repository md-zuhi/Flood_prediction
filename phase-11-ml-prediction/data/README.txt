Phase 11 — ML Prediction data folder

Place the following here when ready:

- training_data.csv       — labelled flash flood event dataset
- feature_definitions.json — canonical feature list + ranges
- model/                  — trained model artefacts (weights, scalers, etc.)
- evaluation/             — confusion matrix, ROC curves, metrics

DO NOT place raw HDF5 / GeoTIFF files here.
Those remain in their respective phase data folders.
